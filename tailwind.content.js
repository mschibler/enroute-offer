/**
 * enroute_offers – Tailwind content paths
 *
 * Add the path below to the `content` array in your theme's tailwind.config.js
 * so that the plugin's template classes are included in the compiled CSS.
 *
 * Example (in your theme's tailwind.config.js):
 *
 *   content: [
 *     './theme/**\/*.{php,js,html}',
 *     // ↓ add this line:
 *     '../plugins/enroute_offers/templates/**\/*.php',
 *   ],
 *
 * After adding, restart `npm run watch` or run `npm run build`.
 */
module.exports = {
  // This file is for documentation only.
  // Copy the path above into your theme's tailwind.config.js content array.
};
